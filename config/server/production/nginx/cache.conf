# Static files on public
if (-f $document_root/$uri) {
    rewrite (.*) $uri break;
}

location ~* ^.+.(jpg|jpeg|gif|png|ico|css|tgz|gz|js|swf)$ {
    expires +10d;
    if (-f $document_root/$uri) {
        rewrite (.*) $uri break;
    }
}